#include <iostream>
#include "LogicHelper.h"
#include "Character.h"

int main()
{
/*
    // 캐릭터 생성 Test
    Character& character = Character::GetInstance();
    character.Display();

    // 레벨업 테스트
    int i = 0;
    while (i < 4)
    {
        int exp = 0;
        std::cin >> exp;
        character.SetExp(exp);
        if (exp >= 100)
        {
            character.LevelUp();
            
        }
        character.Display();
        i++;
    }
 */
}